<%= form_with(model: train, local: true) do |form| %>
    <% if train.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(train.errors.count, "error") %> prohibited this train from being saved:</h2>

          <ul>
            <% train.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name, id: :train_name %>
    </div>

    <div class="field">
      <%= form.label :timetable_id %>
      <%= form.select :timetable_id, Timetable.all.collect { |n| [n.name, n.id] } %>

    </div>
    <div class="actions">
      <%= form.submit %>
    </div>
<% end %>
