<%= form_with(model: timetable, local: true) do |form| %>
  <% if timetable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(timetable.errors.count, "error") %> prohibited this timetable from being saved:</h2>

      <ul>
      <% timetable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name %>
    </div>

  <div class="field">
    <%= form.label :arrival_time %>
    <%= form.datetime_select :arrival_time, :as => :datetime_picker %>
  </div>

  <div class="field">
    <%= form.label :departure_time %>
    <%= form.datetime_select :departure_time,:as => :datetime_picker %>
  </div>


    <div class="field">
      <%= form.label :station_id %>
      <%= form.select :station_id , Station.all.collect { |s| [s.name, s.id] } %>

    </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
